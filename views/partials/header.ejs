<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= typeof pageTitle !== 'undefined' ? pageTitle : settings.hospitalName %> | <%= settings.hospitalName %></title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" 
          rel="stylesheet" 
          integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" 
          crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="/css/style.css">
    
    <script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.2/dist/chart.umd.min.js"></script> 
    
    <style>
        .navbar {
            background: linear-gradient(90deg, #0077b6, #0096c7);
            box-shadow: 0 3px 10px rgba(0, 0, 0, 0.1);
        }

        .navbar-brand {
            font-weight: 600;
            font-size: 1.4rem;
            color: #fff !important;
            letter-spacing: 0.5px;
        }
        /* Use Hospital Name from settings here */
        .navbar-brand:before {
            content: "<%= settings.logoUrl.startsWith('/images/default_logo') ? '🏥' : '' %>";
            margin-right: 5px;
        }
        

        .navbar-nav .nav-link {
            color: #f1f1f1 !important;
            margin-left: 12px;
            transition: all 0.3s ease;
            font-weight: 500;
        }

        .navbar-nav .nav-link:hover {
            color: #fff !important;
            text-decoration: underline;
        }

        .navbar-nav .nav-link.active {
            color: #fff !important;
            font-weight: 600;
            border-bottom: 2px solid #fff;
        }

        /* Responsive fix */
        @media (max-width: 991px) {
            .navbar-collapse {
                background-color: #0096c7;
                border-radius: 0 0 10px 10px;
                padding: 10px;
            }
        }
    </style>
</head>

<body class="d-flex flex-column min-vh-100"> 
    <header class="sticky-top">
        <nav class="navbar navbar-expand-lg navbar-dark">
            <div class="container-fluid container">
                <a href="/" class="navbar-brand"><%= settings.hospitalName %></a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                    aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <div class="collapse navbar-collapse" id="navbarNav">
                    
                    <ul class="navbar-nav me-auto">
                        <% if (isLoggedIn) { %> 
                            <li class="nav-item"><a href="/" class="nav-link <%= pageTitle === 'Hospital Dashboard' ? 'active' : '' %>"><i class="bi bi-house-door-fill me-1"></i> Dashboard</a></li>
                            <li class="nav-item"><a href="/patients" class="nav-link <%= pageTitle === 'Patients List' ? 'active' : '' %>"><i class="bi bi-person-wheelchair me-1"></i> Patients</a></li>
                            <li class="nav-item"><a href="/doctors" class="nav-link <%= pageTitle === 'Doctors List' ? 'active' : '' %>"><i class="bi bi-heart-pulse me-1"></i> Doctors</a></li>
                            <li class="nav-item"><a href="/appointments" class="nav-link <%= pageTitle === 'Appointments List' ? 'active' : '' %>"><i class="bi bi-calendar-check me-1"></i> Appointments</a></li>
                            <li class="nav-item"><a href="/billing" class="nav-link <%= pageTitle === 'All Invoices' ? 'active' : '' %>"><i class="bi bi-receipt me-1"></i> Billing</a></li>
                            
                            <% if (user.role === 'Admin') { %>
                                <li class="nav-item">
                                    <a href="/services" class="nav-link <%= pageTitle === 'Service Management' || pageTitle === 'Edit Service' ? 'active' : '' %>">
                                        <i class="bi bi-list-check me-1"></i> Services
                                    </a>
                                </li>
                                <li class="nav-item"><a href="/users" class="nav-link <%= pageTitle === 'User Management' ? 'active' : '' %>"><i class="bi bi-people-fill me-1"></i> Users</a></li>
                                <li class="nav-item">
                                    <a href="/settings" class="nav-link <%= pageTitle === 'System Settings' ? 'active' : '' %>">
                                        <i class="bi bi-gear-fill me-1"></i> Settings
                                    </a>
                                </li>
                            <% } %>
                        <% } %>
                    </ul>
                    
                    <ul class="navbar-nav ms-auto">
                        <% if (isLoggedIn) { %> 
                            <li class="nav-item me-3 d-flex align-items-center text-white small">
                                <i class="bi bi-person-circle me-1"></i> Hello, **<%= user.name %>** (<%= user.role %>)
                            </li>
                            <li class="nav-item">
                                <form action="/logout" method="POST" class="d-inline">
                                    <button type="submit" class="btn btn-sm btn-outline-light fw-bold">Logout</button>
                                </form>
                            </li>
                        <% } else { %>
                            <li class="nav-item">
                                <a href="/login" class="btn btn-sm btn-outline-light fw-bold">Login</a>
                            </li>
                        <% } %>
                    </ul>
                </div>
            </div>
        </nav>
    </header>
    
    <main class="container mt-4 flex-grow-1">